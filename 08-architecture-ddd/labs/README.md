# Labs — Level 8 — Architecture Styles, Boundaries & Domain-Driven Design

- [ ] run an event-storming session (even solo) on a domain you know. Produce a context map, then propose a service decomposition and mark which invariants become eventual.
- [ ] extract one service from a monolith using strangler fig + anti-corruption layer + dual-write, with a documented rollback plan at each step.
