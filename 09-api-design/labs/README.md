# Labs — Level 9 — API & Interface Design

- [ ] design a complete API for a booking system: cursor pagination, ETag concurrency, idempotency keys, RFC 9457 errors, and a versioning/deprecation policy. Lint it with Spectral.
- [ ] expose the same domain over REST, gRPC, and GraphQL. Measure payload size and latency, then list what each made easy and hard.
